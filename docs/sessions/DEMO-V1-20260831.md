# Demo V1 — phiên triển khai và kiểm chứng 2026-08-31

Status: IMPLEMENTED
Evidence Level: E1
Executed By: Codex
Timestamp: 2026-08-31T15:30:24+07:00

## Phạm vi và nhánh

Yêu cầu Owner: một phiên xuyên suốt workbook kế toán → production → Excel
với AUTO/Review Queue. Scope và gate được ghi trước code tại
`docs/tasks/TASK-DEMO-V1.md`. Không Tracking/Firebase, không đổi giá, authority,
engine, schema hay business rule. Không merge; không tạo task tiếp theo.

Đầu phiên: detached HEAD, SHA `1ab5dbdfdd70deff1f0636ec1bb5f734ba6a0592`.
Nhánh kỳ vọng `claude/bh73804-confirmed-identity` trỏ cùng SHA nhưng nằm ở
worktree khác. Tạo `codex/demo-v1` từ đúng commit này. Giữ baseline Owner
chỉ định thay vì đồng bộ sang nhánh mặc định khác theo hướng dẫn lịch sử.

15 tệp chưa theo dõi đã được báo trước sửa: tám tệp trong
`artifacts/post_cutover_preflight_BH73804/`, hai `data/captures/PPH-*.json`,
bốn `data/tracking_catalog/capture*.json`, một
`data/tracking_price_history/capture_contract_v1_prod_2.json`.
Sau kiểm thử, cả 15 tệp khớp byte với bản tại checkout gốc
`/Users/hoangvinh/Documents/Reports`. Không tệp nào được sửa/xóa/commit.
Production store tạo thêm `data/product_identity/mappings.jsonl.lock`, cũng
để nguyên và không commit. Workbook đầu ra được gitignore sẵn.

## Triển khai

- `app/demo.py`: nhận bốn đường dẫn bắt buộc, nạp capture qua loader hiện có,
  đặt `public_purchase=None`, gọi `run_import_production`, giữ nguyên result
  và `composition.records` trong `DemoRun`. Không network.
- `app/modules/exporting/excel_exporter.py`: chiếu kết quả từng dòng và
  provenance sang ba sheet, không tính giá/lợi nhuận lại. Đối chiếu multiset
  tệp/sheet/dòng/OrderID/hash trước khi xuất; thiếu/dư dòng hay bản ghi giá
  không được báo hoàn tất. Không ghi đè output đã tồn tại.
- Review Queue giữ reason/detail có sẵn; missing KPI chỉ được ghi là chưa
  có kết quả khi engine không phơi bày nguyên nhân cụ thể. Finding cấp lô
  đếm riêng, không gán nhầm cho mọi đơn. Giá tiền trống giữ nghĩa Pending.
- Hướng dẫn Owner: `docs/demo-v1.md`. Không thêm dependency vào pyproject;
  dùng openpyxl/PyYAML/pytest đã được repo khai báo.

## Kiểm thử và regression

Python mặc định thiếu dependency, venv cũ thiếu pytest. Đã tạo `.venv` riêng
trong worktree và cài đúng dependency hiện có (không commit môi trường).

```text
.venv/bin/python -m pytest -q tests/test_demo.py
13 passed in 1.38s
```

Bao phủ: production thật, đa dòng, key bản ghi trùng, ngày thiếu, capture
trước ngày bán, PP YAML không bị đọc, KPI chưa đủ điều kiện, giữ nguồn giá
lịch sử, thiếu dòng/đơn/record phải lỗi, CLI từ thư mục khác, output tồn tại,
capture mất/hỏng/FAILED không lộ payload, text không thành công thức Excel,
đầu vào không bị sửa.

Lượt full suite đầu tiên tại worktree có runtime: 1300 passed, 11 skipped,
4 failed. Hai test cũ giả định catalog chưa tồn tại nhưng artifact local
`data/tracking_catalog/capture.json` đã có với trạng thái FAILED; hai test
HTTP không bind được localhost trong sandbox. Không sửa test/engine/capture.

Lượt archive tạm: hai kiểm tra Git thiếu `.git`, còn 1302 passed, 11 skipped.
Đã thay bằng **clone Git cục bộ sạch**, checkout đúng SHA đầu phiên rồi chép
chính source/test Demo V1 sang (không chép runtime). Chạy với quyền localhost:

```text
cwd=/private/tmp/reports-demo-v1-clean
/Users/hoangvinh/.codex/worktrees/37ab/Reports/.venv/bin/python -m pytest -q
1305 passed, 11 skipped in 11.93s
```

Không bỏ chọn test hay thay expected output. 11 skipped thuộc suite hiện có.
Không có khác biệt trên các file engine/config/data đã theo dõi.

## Chạy dữ liệu thật

Workbook Golden đã ẩn danh từ dữ liệu thật, không dữ liệu dựng để tạo AUTO:

```sh
.venv/bin/python app/demo.py \
  --sales tests/fixtures/golden/period_2026_01.xlsx \
  --tracking-capture data/captures/PPH-20260831T080038Z.json \
  --tracking-catalog data/tracking_catalog/capture_contract_v1_prod_2.json \
  --output outputs/demo-v1/report.xlsx
```

```text
DEMO_COMPLETE
OUTPUT=/Users/hoangvinh/.codex/worktrees/37ab/Reports/outputs/demo-v1/report.xlsx
ORDERS=254
AUTO=1
REVIEW_QUEUE=253
ORDER_ACCOUNTING_RATE=100%
```

Đối chiếu độc lập từ raw reader sang chính XLSX đã ghi: 254/254 OrderID và
351/351 cặp OrderID/dòng nguồn, không mất hoặc nhân đôi. Review Queue chứa
349 dòng cần xem + 7 finding cấp lô. Tổng doanh thu đã xác định: 3.562.310.000
VND; KPI đủ điều kiện: 900.000 VND từ hai dòng. Chỉ một đơn có toàn bộ dòng
AUTO. Giữ giá lịch sử có nguồn riêng, không biến thành Tracking PP.

SHA-256:

```text
sales=73b519aa930c59fda8b06f0763951b0d1859b53a53f8bde8069b20af76e7adcb
history=03905243fa901ef0feb5e9daf58ad83ee8dcfb296546229128f66c0e5138d06a
catalog=229a53c1dda002dcf626956171011a7d0ea7d9b4f47fc79b8047321a7eb2f100
output=035c266917ca8b12d52415692db148fa31deafbcf52a70a6964babcb59f0e6c0
```

Kiểm tra bổ sung workbook preflight có sẵn
`/Users/hoangvinh/Documents/Reports/tmp/post_cutover_preflight/BH73804.xlsx`,
giữ nguyên ngày 2026-09-01 và cùng hai capture: ORDERS=1, AUTO=0,
REVIEW_QUEUE=1, ORDER_ACCOUNTING_RATE=100%. Namespace TRACKING, mã T2109NT1G,
reason `TRACKING_HISTORY_PENDING`, chi tiết Tracking
`SNAPSHOT_DOES_NOT_COVER_SALE_INTERVAL`. Không đổi dữ liệu/capture để ép AUTO.

Đã đọc lại số liệu, quét lỗi công thức (0 lỗi), render và xem cả ba sheet,
bao gồm vùng provenance phía phải của Review Queue. Filter, freeze panes,
định dạng VND/ngày và trạng thái AUTO/PENDING hiển thị đúng; không có chart.

## Handoff

Các check Demo V1 đạt. `git diff --check` và `git diff --cached --check`
đều PASS trước commit. Chỉ stage
source/tests/docs có chủ đích; không runtime, XLSX, venv, lock. SHA cuối
được trả trong kết quả phiên bằng `git rev-parse HEAD` sau commit.

Blocker Demo còn lại: không. Deferred: giả định môi trường của hai regression
test cũ; không refactor chúng trong phiên này. Thiếu bằng chứng giá của các
đơn Pending là tình trạng dữ liệu được thể hiện trung thực, không blocker
để Owner dùng Demo. Câu chữ aggregate cũ nhắc Price Master được giữ nguyên
theo production, không sửa renderer ngoài phạm vi. Mức hoàn tất phạm vi
Demo: 100%. Hành động tiếp theo: Owner mở báo cáo và xem các đơn cần bổ sung
bằng chứng; không đề xuất dashboard.
